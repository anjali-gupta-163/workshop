import page2_json from '../data/CONTENTLISTINGPAGE-PAGE2.json';

function page2(state = page2_json.page, action){
    switch(action.type){
        default:
            return state;
    }
}
export default page2;
