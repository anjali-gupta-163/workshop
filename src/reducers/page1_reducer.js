import page1_json from '../data/CONTENTLISTINGPAGE-PAGE1.json';

function page1(state = page1_json.page, action){
    switch(action.type){
        default:
            return state;
    }
}
export default page1;